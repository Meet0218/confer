import PagePlaceholder from "../components/PagePlaceholder";

export default function ScheduleCall() {
  return (
    <PagePlaceholder
      title="Schedule a call"
      message="This page will contain the scheduling form, participant invites, and call creation workflow."
    />
  );
}
