import PagePlaceholder from "../components/PagePlaceholder";

export default function CallHistory() {
  return (
    <PagePlaceholder
      title="Call history"
      message="Completed calls, transcripts, saved drawing snapshots, and AI summaries will be listed here once the calls API exposes history data."
    />
  );
}
