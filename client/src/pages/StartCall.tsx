import PagePlaceholder from "../components/PagePlaceholder";

export default function StartCall() {
  return (
    <PagePlaceholder
      title="Start a call"
      message="This page will host the room creation and Twilio join flow. The home page is already wired here for the next implementation step."
    />
  );
}
